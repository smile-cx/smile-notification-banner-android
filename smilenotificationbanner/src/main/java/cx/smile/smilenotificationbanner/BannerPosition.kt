package cx.smile.smilenotificationbanner

/**
 * Banner position - only TOP is supported
 * @deprecated Position parameter is no longer needed as banners only appear at TOP
 */
@Deprecated("Position parameter is no longer needed")
enum class BannerPosition {
    TOP
}
